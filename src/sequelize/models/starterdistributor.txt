'use strict';
module.exports = (sequelize, DataTypes) => {
  const StarterDistributor = sequelize.define('StarterDistributor', {
    username: {
      primaryKey: true,
      type: DataTypes.STRING
    },
    upLineUsername: {
      type: DataTypes.STRING
    },
    position: {
      type: DataTypes.ENUM,
      values: ['left', 'right'],
      defaultValue: 'left'
    },
    createdAt: {
      allowNull: false,
      type: DataTypes.BIGINT,
      defaultValue: new Date().getTime()
    },
    updatedAt: {
      allowNull: false,
      type: DataTypes.BIGINT,
      defaultValue: new Date().getTime()
    }
  });
  StarterDistributor.associate = function(models) {
    // associations can be defined here
  };
  return StarterDistributor;
};